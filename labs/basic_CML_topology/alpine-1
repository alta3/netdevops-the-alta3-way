# this is a shell script which will be sourced at boot
hostname alpine-1
echo "127.0.0.1   alpine-1" >>/etc/hosts
USERNAME=cisco
PASSWORD=cisco
ip addr add 10.10.2.11/24 dev eth0
ip route add default via 10.10.2.1
